<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cursoavanzadoe1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.DBLDActivity"></activity>
        <activity android:name=".ui.DataBindingActivity" />
        <activity android:name=".ui.LiveDataActivity" />
        <activity android:name=".ui.UserViewModelActivity" />
        <activity
            android:name=".ui.ViewModelActivity"
            android:label="@string/title_activity_view_model"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>